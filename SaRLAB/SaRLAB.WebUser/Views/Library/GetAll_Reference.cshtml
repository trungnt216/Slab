@model IEnumerable<SaRLAB.Models.Entity.Reference>
@{
    Layout = null;
    var type = TempData["type"];
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=0.5" />
    <title>Library- Management</title>
    <!-- css -->
    <link rel="stylesheet" href="~/css/styles.css" />
    <!-- icon-tab -->
    <link rel="icon" href="~/assets/icon/icon_tab.png" />
    <!-- font -->
    <script src="https://kit.fontawesome.com/f0add9272d.js"></script>
</head>

<body>
    <!---head-->
    <header>
        <div class="infor">
            <img src="@Url.Content(TempData["AvtPath"].ToString())" alt="">
            @if (TempData["name"] != null && TempData["name"] is string name && !string.IsNullOrWhiteSpace(name))
            {

                        <a asp-controller="HomePage" asp-action="Information" asp-route-subjectID="1">
                        @Html.Raw(name)
                        </a>

            }
            else
            {
                    <h1>Unknown User</h1>
            }
        </div>

        <div class="search">
            <i class="fa-solid fa-house" onclick="window.location.href='@Url.Action("Index", "HomePage")'"></i>
            @* <div class="tk">
                <input type="search" placeholder="Tìm kiếm">
                <i class="fa-solid fa-magnifying-glass"></i>
            </div> *@
            <i class="fa-solid fa-bars" id="menu-btn"></i>
        </div>
    </header>

    <div class="menu_head">
        <a asp-action="GetAll_Document" asp-controller="HomePage" asp-route-titleDocument="giới thiệu">Giới thiệu</a>
        <a asp-action="GetAll_Document" asp-controller="HomePage" asp-route-titleDocument="văn bản biểu mẫu">Văn bản - Biểu mẫu</a>
        <a asp-action="GetAll_Document" asp-controller="HomePage" asp-route-titleDocument="hướng dẫn sử dụng">Hướng dẫn sử dụng</a>
        <a asp-action="GetAll_Document" asp-controller="HomePage" asp-route-titleDocument="hình ảnh">Kho hình ảnh</a>
        <a asp-action="GetAll_Document" asp-controller="HomePage" asp-route-titleDocument="liên hệ">Liên hệ</a>
    </div>

    <div class="olay" id="olay"></div>

     <!---head-->

    <div class="container" id="li-index">
        <!-- header -->
        <div id="header-placeholder"></div>
        <!-- main -->
        <main>
            <!-- nav -->
            <nav>
                <div class="logo">
                    <h1>SaRLAB</h1>
                </div>
                <div class="menu">
                    <a id="active" href="" onclick="window.location.href='@Url.Action("Index", "HomePage")'">Trang
                        chủ</a>
                    <a href="" onclick="window.location.href='@Url.Action("Logout", "HomePage")'">Đăng xuất</a>
                </div>
            </nav>
            <!-- title -->
            <div class="title_li" id="list-title">
                <!-- add -->
                <button id="add"
                    onclick="window.location.href='@Url.Action("Create_Reference", "Library", new { type = type })'">
                    <span>Thêm tài liệu</span>
                </button>

                <div class="f-li">
                    <img id="li" src="~/assets/img/li.png" alt="" />
                    <div class="des-li">
                        <h1>-</h1>
                        <h1 id="lxf">@type</h1>
                    </div>
                </div>
                <img src="~/assets/icon/icon_fly.png" alt="" />
            </div>

            <!-- list-book -->
            <div class="book-container">
                <div class="books">
                    @foreach (var item in Model)
                    {
                        <a class="book-item"
                            onclick="window.location.href='@Url.Action("Details_Reference", "Library", new {id = item.ID , type = type })'">
                            <img src="@Url.Content(item.CoverImage)" alt="Cover Image">
                            <div class="xx">
                                <div class="an">
                                    <p>
                                        Tác giả: @Html.DisplayFor(modelItem => item.Author)<br />
                                        Tên: @Html.DisplayFor(modelItem => item.Name)<br />
                                        @Html.DisplayFor(modelItem => item.Summary)
                                    </p>
                                </div>
                            </div>
                        </a>
                    }
                </div>
            </div>
            <div class="ke">
            </div>

            <div class="pagination"></div>
        </main>
    </div>
    <!-- scripts -->
    <script src="~/js/scripts.js" defer></script>
    <script src="~/js/lab.js" defer></script>
</body>

</html>
